apiVersion: 1

providers:
  - name: Redis Demo
    orgId: 1
    folder: Redis Demo
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 5
    options:
      path: /etc/grafana/provisioning/dashboards/json
